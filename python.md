# Python

## 目标

能够用 Python 构建可靠的小工具、数据分析和 AI 管线，并解释环境、类型、测试、错误处理、并发、持久化和发布取舍。

## 已有实践

`agent` 展示兼容 Anthropic Messages API 的调用、环境变量、CLI 和 Tool Use；提交历史出现多轮循环、并行工具、向量库与 RAG。

`mediabrief` 展示 FastAPI、SQLite、SSE、统一队列、字幕优先、Whisper fallback、LLM 清洗与总结、重试、导出和前后端测试。

## 学习路线

第一阶段复习语法、数据结构、函数、模块、文件和异常；第二阶段加入类型标注、测试、日志、配置和包管理；第三阶段学习 SQLite、HTTP、异步和任务状态；第四阶段完成一个可安装、可测试、可观测的工具。

## 本仓库项目

`learning_tracker.py` 使用标准库实现 SQLite 学习记录、汇总和 CSV 导出；`test_learning_tracker.py` 提供回归测试。

扩展功能时先写通过标准，再加测试；优先候选包括目标管理、连续学习天数、Markdown 周报、主题别名和 JSON 导出。

## 项目复盘重点

工具 schema 是否明确，模型循环如何停止，兼容 API 差异如何隔离，任务是否幂等，重试是否重复昂贵步骤，状态机如何处理取消，LLM 输出怎样结构化验证。

## 阶段产出

一个标准库 CLI，一个包含类型与测试的小包，一份真实数据分析，以及一篇从 `agent` 或 `mediabrief` 提炼的系统设计复盘。

## 下一步

运行全部测试，添加五条真实学习记录，选择一个小功能，通过先写测试的方式实现。
