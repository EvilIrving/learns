# AI Agent 与语音转录

## 已有实践

工具调用、多轮循环、并行工具、向量检索、RAG、兼容 API、字幕提取、Whisper、LLM 清洗与总结、队列、SSE、重试和结构化输出测试。

## 学习地图

模型边界，工具 schema，编排循环，状态和记忆，检索与上下文，异步任务，幂等与重试，成本与延迟，评估、安全和隐私。

## 关键区分

能调用工具不等于 Agent 可靠；有向量库不等于检索有效；生成摘要不等于摘要正确；进度条存在不等于状态机一致。

## 阶段产出

一张端到端数据流图，一份状态机说明，一组失败案例，一个离线评估集，以及对成本、延迟和正确性的权衡记录。

## 下一步

从 `agent` 选择一次工具调用链，从 `mediabrief` 选择一次转录任务链，分别写出状态、失败点、重试边界和测试方法。
