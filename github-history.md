# GitHub 历史映射

以下是从可访问仓库和提交记录中验证出的实践方向。

`web-pronunciation` 对应英语发音、登录、主题、速率、跟踪脚本、音标 provider 轮询和 LLM fallback。

`Pension-Estimation` 对应养老金计算、个人财务和数据模型。

`ai-transcriber` 对应 Whisper、本地测速、任务队列、重试、进度和 schema fallback。

`agent` 对应 Python、LLM Agent、工具调用、向量库、RAG、多轮会话和并行工具执行。

`cbti-sleep` 对应 CBT-I、睡眠日记、睡眠窗口、每日任务和健康产品设计。

`light-stats` 对应 Swift/macOS、SwiftUI、Core Animation、Canvas、主题架构和渲染性能。

`swift-fitness` 与 `exercise-weapp` 证明存在健身产品实践，但具体知识结论仍需后续按代码和文档整理。
